/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "192.168.0.109",
        "localhost",
        "api.betterfuture.uz",
          "api.qrserver.com"
        ]
      },
}

module.exports = nextConfig
